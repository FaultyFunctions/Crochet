import type { Edge, Node } from '@xyflow/svelte';

// TODO: Implement Chatter Parser

export function parseChatter(lines: string[]): { nodes: Node[]; edges: Edge[] } {
	// Parsing stuff
	return { nodes: [], edges: [] };
}
